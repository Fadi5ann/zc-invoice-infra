route:
  receiver: 'default-receiver'
  group_wait: 10s
  group_interval: 1m
  repeat_interval: 1h

receivers:
  - name: 'default-receiver'
    # في المستقبل، يمكن إضافة Webhook هنا لإرسال رسالة إلى هاتف الفريق N1